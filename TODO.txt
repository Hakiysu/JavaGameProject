开心消消乐游戏需求：
1.所有参与角色：熊，鸟，狐狸，青蛙
2.功能：
    1.由系统随机生成元素，并显示在窗口中（8行6列），保证初始化后没有可消除的元素
    2.选中2个元素，如果它们相邻，则交换，之后进行判断；
    if不可消除（判断条件：不可消：3个以下；可消除：3个或3个以上相同元素连成一条线），则交换回来
    if可消除，则
        1.播放爆破动画后消除元素
        2.随机生成新元素，如果还是可消，重复可消除if分支，直到不可消